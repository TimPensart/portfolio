import React from "react";
import styled from "styled-components";
import { IconStyled } from "./scroll-styling";

export const IconAnimation = () => {
    return(
        <IconStyled>helllo</IconStyled>
    );
}